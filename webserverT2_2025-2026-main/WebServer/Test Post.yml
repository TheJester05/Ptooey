info:
  name: Test Post
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:3000/test-jwt
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
